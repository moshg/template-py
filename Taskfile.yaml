version: '3'

tasks:
  run:
    desc: Run the main script
    cmds:
      - uv run -m template_py.main
  check:
    desc: Check code quality (type + lint + format)
    deps:
      - check:type
      - check:lint
      - check:format
  check:type:
    desc: Typecheck the codebase
    cmds:
      - uv run pyright src
  check:lint:
    desc: Lint the codebase
    cmds:
      - uv run ruff check src
  check:format:
    desc: Check code formatting
    deps:
      - check:format:python
      - check:format:toml
  check:format:python:
    desc: Check Python formatting
    cmds:
      - uv run ruff format --check src
  check:format:toml:
    desc: Check TOML formatting
    cmds:
      - uv run tombi format --check
  fix:
    desc: Fix code issues (lint + format)
    cmds:
      - task: fix:lint
      - task: fix:format
  fix:lint:
    desc: Fix linting issues
    cmds:
      - uv run ruff check --fix src
  fix:format:
    desc: Format the codebase
    deps:
      - fix:format:python
      - fix:format:toml
  fix:format:python:
    desc: Format Python files
    cmds:
      - uv run ruff format src
  fix:format:toml:
    desc: Format TOML files
    cmds:
      - uv run tombi format
  test:
    desc: Run the test suite
    cmds:
      - uv run pytest
  audit:
    desc: Audit dependencies for vulnerabilities
    cmds:
      - uv run --all-groups pip-audit
